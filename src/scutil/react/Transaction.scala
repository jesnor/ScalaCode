package scutil.react

import scala.collection.mutable.HashMap

trait Transaction extends Disposable {
//  val observers = new HashMap[Observer[T], HashMap[Observable[T], T] forSome { type T }]
  
  def dispose {
    
  }
}
